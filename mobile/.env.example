# Production or manually managed API endpoint. For local development, use
# `npm start`; it automatically writes the current LAN address to .env.local.
EXPO_PUBLIC_API_URL=https://api.example.com/api
